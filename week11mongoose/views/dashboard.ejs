<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <h1>
        Users
    </h1>

    <div>
        <ul>
            <% for (usr in users){ user=users[usr] %>
                <li><%= user.username %> : <%= user.password %> - <a href="/delete/<%= user._id %>">Delete</a></li>
                <br>
            <% } %>
        </ul>
    </div>
    
</body>
</html>